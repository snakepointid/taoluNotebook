<android.support.v7.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/tb_editable"
    android:layout_width="match_parent"
    android:layout_height="44dp"
    android:background="?attr/colorPrimary"
    app:layout_constraintTop_toTopOf="parent">
    <LinearLayout
        android:id="@+id/lly_search_bar"
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:layout_marginVertical="@dimen/policy_feedback_margin"
        android:background="@drawable/rect_search"
        android:gravity="center"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:orientation="horizontal">
        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:src="@drawable/ic_find_tb"
            android:background="@color/transparent"/>
        <EditText
            android:id="@+id/et_search_pad"
            android:layout_width="fill_parent"
            android:layout_height="match_parent"
            android:background="@color/transparent"
            android:textSize="@dimen/search_text_size"
            android:textColor="@color/gray_deep"
            android:hint="@string/search_hint" />
    </LinearLayout>
</android.support.v7.widget.Toolbar>